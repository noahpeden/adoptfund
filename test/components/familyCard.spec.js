import React from 'react'
import { shallow, mount } from 'enzyme'
import { expect, assert } from 'chai'
import sinon from 'sinon'

import FamilyCard from '../../public/lib/components/FamilyCard'

describe('FamilyCard', () => {
  // it('should have a state of newUser that is an empty string', () => {
  //   const wrapper = shallow(<FamilyCard />)
  //   expect(wrapper.state().email).to.deep.Equal('');
  // });
  //
  // it('should allow props to be set', () => {
  //   const wrapper = mount(<FamilyCard prop="setProp" />);
  //   expect(wrapper.props().prop).toEqual('setProp');
  //   wrapper.setProps({ prop: 'newProp' });
  //   expect(wrapper.props().prop).toEqual('newProp');
  // });
//   it("contains spec with an expectation", function() {
//   expect(shallow(<FamilyCard />).contains(<div/>)).to.equal(true);
// });
});
